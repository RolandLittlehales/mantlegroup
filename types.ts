/**
 * Generic type for counting unique occurances of strings
 */
export type UniqueCount = {
    [key: string]: number;
};

export type URLVists = UniqueCount;

export type IPVists = UniqueCount;
